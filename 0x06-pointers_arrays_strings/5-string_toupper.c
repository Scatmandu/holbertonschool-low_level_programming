#include "holberton.h"
/**
*string_toupper- translates lowercase to upperscase in a string
*@str: pointer to a string
*Return: 0
**/
char *string_toupper(char *str)
{
	char *str;
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		if (s[i] >= 'a' && s[i] <= 'z')
		{
			s[i] = s[i] - 32;
		}
	}
return (0);
}
